[phases.setup]
nixPkgs = ["python311", "git-lfs"]

[phases.install]
cmds = [
  "git lfs install",
  "git lfs pull",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
